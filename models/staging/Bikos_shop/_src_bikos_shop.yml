sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        # config:
        #   freshness:
        #     warn_after:
        #       count: 1
        #       period: days
        #     error_after:
        #       count: 1
        #       period: week
        #   loaded_at_field: _etl_loaded_at

