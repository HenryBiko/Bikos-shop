models:
  - name: stg_biko_shop_customers
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null
  - name: stg_biko_shop_orders
    columns:
      - name: order_id
        data_tests:
          - unique
          - not_null
      # - name: order_status
      #   data_tests:
      #     - accepted_values:
      #         values:
      #           - completed
      #           - shipped
      #           - placed
      #           - returned_pending
      #           - returned

      - name: customer_id
        data_tests:
          - relationships:
              field: customer_id
              to: ref('stg_biko_shop_customers')
